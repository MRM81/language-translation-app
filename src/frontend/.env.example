# Backend base URL — used by the Vite proxy configuration.
# The default backend port from launchSettings.json is 5074.
# Copy this file to .env.local and update if your backend runs on a different port.
# Do not commit .env.local — it is excluded by .gitignore.
VITE_API_BASE_URL=http://localhost:5074
